# 작업 가이드

## 작업 흐름

1. 작업을 시작하기 전에 프로젝트 구조, 기존 아키텍처, 관련 구현을 먼저 확인한다.
2. 확인한 내용을 바탕으로 문제, 영향 범위, 가능한 변경 방향을 간결하게 정리해 사용자에게 알린다.
3. 사용자와의 대화를 통해 실제 적용 범위와 구현 방향을 결정한 뒤 수정한다.
4. 변경 후에는 합의한 범위에 맞는 필수 검증만 수행하고 결과를 공유한다.

## 구현 원칙

- 기존 아키텍처와 코드 흐름을 존중한다. 새 구조나 추상화는 현재 구조로 해결하기 어렵고, 그 이점이 분명할 때만 도입한다.
- 새 기능을 독립된 코드로 덧붙이기보다 기존 로직과 공통 구성 요소를 우선 활용한다.
- 요구 사항을 충족하는 최소한의 변경을 선호한다. LLM 특유의 과도한 계층화, 중복된 헬퍼, 불필요한 일반화는 피한다.
- 코드는 의도가 바로 드러나도록 간결하고 명확하게 작성한다. 기능과 무관한 장황한 설명, 방어 코드, 예외 처리는 추가하지 않는다.

## 테스트 원칙

- 변경된 동작과 의도를 검증하는 데 필요한 테스트만 추가하거나 수정한다.
- 테스트는 무엇을 보장하는지 쉽게 이해할 수 있어야 한다. 의도를 흐리거나 중복되는 장황한 테스트는 피한다.
- 기존 테스트는 관련 변경에 필요한 경우에만 수정한다.
